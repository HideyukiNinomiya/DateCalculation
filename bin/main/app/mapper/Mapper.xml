<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="app.mapper.Mapper">
    <select id="selectAll" resultType="app.domain.DateCalculation">
        SELECT date_id FROM date_calculation
        SELECT date_name FROM date_calculation
    </select>
</mapper>